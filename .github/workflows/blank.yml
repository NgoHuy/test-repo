name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: id
    - name: Run a multi-line script
      run: |
        ls -la /data /tmp /imagegeneration /var
        grep -i Authorize /data /imagegeneration /var /tmp -r
